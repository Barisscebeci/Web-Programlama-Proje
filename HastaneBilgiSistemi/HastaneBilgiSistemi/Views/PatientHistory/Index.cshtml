@model IEnumerable<HastaneBilgiSistemi.Data.Model.PatientHistory>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">@_localization.Getkey("Create New").Value</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.EndDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Reservation)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Doctor)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Polyclinic)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Diseas)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.EndDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Reservation.StartDate)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Doctor.User.FullName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Polyclinic.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Diseas.Name)
            </td>
            <td>
                    <a asp-action="Edit" asp-route-id="@item.Id">@_localization.Getkey("Edit").Value</a> |
                    <a asp-action="Details" asp-route-id="@item.Id">@_localization.Getkey("Details").Value</a> |
                    <a asp-action="Delete" asp-route-id="@item.Id">@_localization.Getkey("Delete").Value</a>
            </td>
        </tr>
}
    </tbody>
</table>
